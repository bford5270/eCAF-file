# Provisioning Runbook — eCAF SharePoint Lists

Generated by `tools/build_lists.py` from `schemas/lists.json`. Do not hand-edit —
edit the schema and re-run, or your changes are lost on the next build.

**Read this first.** "Create list from CSV" gets you the list and roughly-typed
columns in one action. It cannot set choice values, lookups, person columns,
indexes, required flags, or max length. Every one of those is a manual step below,
and several of them *cannot be applied by changing the imported column* — the
column has to be deleted and recreated. The per-list tables say which.

## What CSV import gets wrong, every time

| Schema type | What import creates | Fix |
|---|---|---|
| `choice` | Single line of text | Change type to Choice, then paste the choice values. Converting text->choice is supported and keeps data. |
| `choiceMulti` | Single line of text | Change to Choice, tick "allow multiple selections". Sample rows use `;` separators — re-enter the 3 sample rows by hand afterwards. |
| `lookup` | Single line of text | **Cannot convert.** Delete the imported column, add a real Lookup column of the same name, re-key the 3 sample rows. Provision parent lists first (order below). |
| `person` | Single line of text | **Cannot convert.** Delete, add Person or Group. Sample values are `@test.invalid` and will not resolve — that is intentional; clear them or replace with test accounts. |
| `date` | Date and time (with time) | Change format to Date Only. |
| `number` | Number, sometimes with decimals | Set 0 decimals. |
| `yesno` | Single line of text ("Yes"/"No") | Change type to Yes/No. |
| `note` | Single line of text (truncates at 255!) | **Change before entering real data.** Multiple lines of text, plain text, appending OFF (appending breaks Patch). |

## Provisioning order (parents before children — lookups depend on it)

1. `eCAF_Holidays`, `eCAF_Config` — no dependencies, and the app will not start without them.
2. `eCAF_Providers`
3. `eCAF_Standards`
4. `eCAF_ReviewerPool`
5. `eCAF_ReviewCases` — looks up Providers, and **self-references** for `PairedCase`.
   Create the list first, then add the `PairedCase` lookup pointing at itself.
6. `eCAF_ReviewResponses`, `eCAF_ReviewSummaries`, `eCAF_ActivityData`
7. `eCAF_Documents` (document library — see the content-type section)
8. `eCAF_Routing` — looks up the document library, so it goes last.

## Per-list steps


### eCAF_Providers — Providers

Import `eCAF_Providers.csv`, then:

| Column | SharePoint type | Settings | Why |
|---|---|---|---|
| `Title` | Single line of text | **required** | Full provider name — CUI environment, no initials |
| `RankGrade` | Single line of text |  |  |
| `Service` | Choice (drop-down) | values: `Navy`, `Army`, `Air Force`, `Civilian`, `Contractor` | Drives which MILDEP guidance applies to EWP events |
| `Specialty` | Choice (drop-down) | **indexed** · values: `PrimaryCare-GMO`, `EmergencyMedicine`, `Orthopedics`, `MentalHealth`, `PhysicalTherapy`, `Dental`, `Surgery`, `Anesthesia`, `Other` | Extend locally; keep in sync with eCAF_Standards.Specialty |
| `Department` | Single line of text | **indexed** |  |
| `UnitUIC` | Single line of text |  |  |
| `ProviderCategory` | Choice (drop-down) | values: `ActiveDuty`, `Civilian`, `Contractor`, `ReserveComponent` |  |
| `PrivilegingStatus` | Choice (drop-down) | values: `Full`, `Supervised`, `FPPE-Initial`, `FPPE-ForCause`, `Expired`, `InProcess` |  |
| `PrivilegeExpiration` | Date and time (Date Only format) | **indexed** | Feeds F3 90-day STOP-EWP watch |
| `OPPEPeriodStart` | Date and time (Date Only format) |  |  |
| `OPPEPeriodEnd` | Date and time (Date Only format) |  |  |
| `FPPEWindowEnd` | Date and time (Date Only format) |  |  |
| `ClinicalSupervisor` | Person or Group (people only) | _delete imported column and recreate_ |  |
| `PrimaryPreceptor` | Person or Group (people only) | _delete imported column and recreate_ |  |
| `AlternatePreceptor` | Person or Group (people only) | _delete imported column and recreate_ |  |
| `CQEnrolled` | Yes/No |  |  |
| `OperationalPlatform` | Yes/No |  | With PrivilegingStatus=Supervised triggers F4 stop-and-resolve (BUMEDNOTE 6000) |
| `CCQASLocation` | Single line of text |  |  |

### eCAF_ReviewCases — Review Cases

> **Break inheritance on this list before importing.**

Import `eCAF_ReviewCases.csv`, then:

| Column | SharePoint type | Settings | Why |
|---|---|---|---|
| `Title` | Single line of text |  | Auto: RC-YYYY-#### via app |
| `Provider` | Lookup | **indexed** · -> `eCAF_Providers` (Title) · _delete imported column and recreate_ |  |
| `FIN` | Single line of text | **required** · max 12 chars | SOLE encounter identifier. No other patient-identifying columns permitted on any list. |
| `EncounterDate` | Date and time (Date Only format) | **required** |  |
| `EncounterType` | Choice (drop-down) | values: `Outpatient`, `Inpatient`, `Procedure`, `Telemedicine`, `Emergency` | Telemedicine flag supports Vol 8 random-sampling requirement for distant-site review |
| `ReviewPurpose` | Choice (drop-down) | **indexed** · values: `FPPE`, `OPPE`, `PAR-Supporting`, `ForCause` |  |
| `ReviewType` | Choice (drop-down) | values: `Peer`, `CrossSpecialty` | Peer = same/similar specialty; only Peer counts in OPPE 10-floor and 455 item 11 denominators (credentials-committee-reversible default) |
| `RequiredReviewerSpecialty` | Choice (drop-down) | values mirror `eCAF_Providers.Specialty` |  |
| `AssignedReviewer` | Person or Group (people only) | **indexed** · _delete imported column and recreate_ |  |
| `AssignedDate` | Date and time (Date Only format) |  |  |
| `Suspense` | Date and time (Date Only format) | **indexed** |  |
| `CaseStatus` | Choice (drop-down) | **indexed** · values: `Assigned`, `InProgress`, `Complete`, `Overdue`, `Reassigned` |  |
| `PairedCase` | Lookup | -> `eCAF_ReviewCases` (Title) · _delete imported column and recreate_ | Links the CrossSpecialty twin when intake creates a pair |

### eCAF_ReviewResponses — Review Responses

> One item per standard per case (long format). Enables standards-driven rendering and per-standard cross-specialty analytics without schema churn.

> **Break inheritance on this list before importing.**

Import `eCAF_ReviewResponses.csv`, then:

| Column | SharePoint type | Settings | Why |
|---|---|---|---|
| `Title` | Single line of text |  |  |
| `ReviewCase` | Lookup | **indexed** · -> `eCAF_ReviewCases` (Title) · _delete imported column and recreate_ |  |
| `Standard` | Lookup | **indexed** · -> `eCAF_Standards` (Title) · _delete imported column and recreate_ |  |
| `Result` | Choice (drop-down) | **required** · values: `MET`, `NOT_MET`, `NA` | Canonical values only — normalize at write time, never store variants |
| `Comment` | Multiple lines of text (plain text, appending OFF) |  | No PHI beyond what the standard requires; FIN lives on the case, not here |

### eCAF_ReviewSummaries — Review Summaries

> One item per completed case: overall determination + the fixed DHA 455 item 11 elements and six-domain constructs that are form-mandated regardless of standards library content.

> **Break inheritance on this list before importing.**

Import `eCAF_ReviewSummaries.csv`, then:

| Column | SharePoint type | Settings | Why |
|---|---|---|---|
| `Title` | Single line of text |  |  |
| `ReviewCase` | Lookup | **indexed** · -> `eCAF_ReviewCases` (Title) · _delete imported column and recreate_ |  |
| `OverallDetermination` | Choice (drop-down) | **required** · values: `Satisfactory`, `Unsatisfactory` |  |
| `Item11Elements` | Multiple lines of text (plain text, appending OFF) |  | JSON blob of the fixed 455 item 11 element results (MET/NOT_MET/NA each) — schema in standards-schema.json |
| `CompetencyDomains` | Multiple lines of text (plain text, appending OFF) |  | JSON blob: 16 constructs across six domains, Satisfactory/Unsatisfactory/NotObserved each |
| `Narrative` | Multiple lines of text (plain text, appending OFF) |  |  |
| `CompletedDate` | Date and time (Date Only format) |  |  |

### eCAF_Standards — Standards Library

Import `eCAF_Standards.csv`, then:

| Column | SharePoint type | Settings | Why |
|---|---|---|---|
| `Title` | Single line of text |  | e.g. DOC-001, PC-GMO-003 |
| `StandardText` | Multiple lines of text (plain text, appending OFF) | **required** |  |
| `Category` | Choice (drop-down) | values: `Documentation`, `ClinicalCare`, `MedicationUse`, `BloodUse`, `ConsultsReferrals`, `Communication`, `ProfessionalKnowledge`, `SystemsPractice`, `MedStaffRule`, `DHABusinessRule` |  |
| `Specialty` | Choice (drop-down) | **indexed** · values: `Universal`, `PrimaryCare-GMO`, `EmergencyMedicine`, `Orthopedics`, `MentalHealth`, `PhysicalTherapy` | Universal renders on every case |
| `SourceCitation` | Single line of text |  | Verbatim doc + section for DHA-sourced; 'Local SOP' or 'CL-validated' otherwise |
| `MapsTo` | Single line of text |  | Semicolon list of form-field tokens per standards-schema.json (e.g. FPPE-3I;OPPE-IIIF) |
| `Status` | Choice (drop-down) | **indexed** · values: `ACTIVE`, `DRAFT-VALIDATE`, `RETIRED` | DRAFT-VALIDATE standards render with a visible draft badge and are excluded from generated documents |
| `ValidatedBy` | Person or Group (people only) | _delete imported column and recreate_ |  |
| `ValidatedDate` | Date and time (Date Only format) |  |  |

### eCAF_ActivityData — Activity Data

Import `eCAF_ActivityData.csv`, then:

| Column | SharePoint type | Settings | Why |
|---|---|---|---|
| `Title` | Single line of text |  |  |
| `Provider` | Lookup | **indexed** · -> `eCAF_Providers` (Title) · _delete imported column and recreate_ |  |
| `PeriodStart` | Date and time (Date Only format) |  |  |
| `PeriodEnd` | Date and time (Date Only format) |  |  |
| `Admissions` | Number (0 decimals) |  |  |
| `OutpatientEncounters` | Number (0 decimals) |  |  |
| `SurgicalProcedures` | Number (0 decimals) |  |  |
| `InvasiveProcedures` | Number (0 decimals) |  |  |
| `NoninvasiveProcedures` | Number (0 decimals) |  |  |
| `AdverseEvents` | Number (0 decimals) |  |  |
| `PctDirectPatientCare` | Number (0 decimals) |  |  |
| `PctIncompleteNotesOver3BD` | Number (0 decimals) |  |  |
| `DaysUnavailable` | Number (0 decimals) |  |  |
| `AppropriateMedUse` | Choice (drop-down) | values: `Y`, `N`, `NA` |  |
| `AppropriateBloodUse` | Choice (drop-down) | values: `Y`, `N`, `NA` |  |
| `RecordReviewsCompleted` | Number (0 decimals) |  |  |
| `RecordReviewsDeficient` | Number (0 decimals) |  |  |

### eCAF_Routing — Endorsement Routing

> **Break inheritance on this list before importing.**

Import `eCAF_Routing.csv`, then:

| Column | SharePoint type | Settings | Why |
|---|---|---|---|
| `Title` | Single line of text |  |  |
| `Document` | Lookup | **indexed** · -> `eCAF_Documents` (Title) · _delete imported column and recreate_ |  |
| `Stage` | Choice (drop-down) | values: `Provider`, `PeerProctor`, `CRCChair`, `DepartmentHead`, `ClinicalLeader`, `PA` | OPPE chain per Template Section X; FPPE/455 chains per their templates |
| `CurrentHolder` | Person or Group (people only) | **indexed** · _delete imported column and recreate_ |  |
| `Action` | Choice (drop-down) | values: `Pending`, `Recommend`, `NotRecommend`, `ReturnWithoutAction` | Mirrors CCQAS Clinical Leader actions |
| `ActionDate` | Date and time (Date Only format) |  |  |
| `Comments` | Multiple lines of text (plain text, appending OFF) |  |  |

### eCAF_ReviewerPool — Reviewer Pool

Import `eCAF_ReviewerPool.csv`, then:

| Column | SharePoint type | Settings | Why |
|---|---|---|---|
| `Title` | Single line of text |  |  |
| `Reviewer` | Person or Group (people only) | **indexed** · _delete imported column and recreate_ |  |
| `QualifiedSpecialties` | Choice (checkboxes, multiple selections allowed) | values mirror `eCAF_Standards.Specialty` |  |
| `Department` | Single line of text |  |  |
| `Available` | Yes/No |  |  |
| `OpenAssignments` | Number (0 decimals) |  | Maintained by F1/F6; used for load-balanced round-robin |

### eCAF_Holidays — Federal Holidays

> Added by this build: app-spec.md loads colHolidays from a holidays list and CLAUDE.md requires business-day math to exclude schemas/holidays.csv; the source lists.json defined no list to hold it. Generated by tools/build_holidays.py from 5 USC 6103.

Import `eCAF_Holidays.csv`, then:

| Column | SharePoint type | Settings | Why |
|---|---|---|---|
| `Title` | Single line of text |  |  |
| `HolidayDate` | Date and time (Date Only format) | **required** · **indexed** | OBSERVED date — business-day math uses this, not StatutoryDate |
| `HolidayName` | Single line of text |  |  |
| `StatutoryDate` | Date and time (Date Only format) |  | Reference only. Differs from HolidayDate when the holiday falls on a weekend |
| `Observed` | Choice (drop-down) | values: `Y`, `N` | Y = shifted for weekend observance |

### eCAF_RoleMap — Role Map

> Added by this build: app-spec.md names eCAF_RoleMap as the documented fallback when the Office 365 Groups connector is blocked, but lists.json did not define it. In a DoD enclave the connector being unavailable is the likely case, not the exception.

> Fallback role resolution ONLY. App-side role gating is UX; the SharePoint group permissions in the provisioning runbook are the actual boundary. A row here grants a user a nicer menu, never access to data.

Import `eCAF_RoleMap.csv`, then:

| Column | SharePoint type | Settings | Why |
|---|---|---|---|
| `Title` | Single line of text | **required** · **indexed** |  |
| `RoleName` | Choice (drop-down) | **required** · values: `MSP`, `ClinicalLeader`, `Reviewer`, `Preceptor`, `Provider` |  |
| `Department` | Single line of text |  | Scopes Clinical Leader dashboard cards to their department |
| `Specialty` | Choice (drop-down) | values mirror `eCAF_Standards.Specialty` | Which specialty's standards this Clinical Leader may validate in S8. Blank for non-CL roles |
| `Active` | Yes/No |  |  |

### eCAF_Config — Configuration

> Added by this build: CLAUDE.md requires the ReviewType denominator rule to be 'data, not hard-code' so the credentials committee can reverse it without an app change. This list is that data.

> Single-key/value settings read once in App.OnStart. MSP write, everyone read.

Import `eCAF_Config.csv`, then:

| Column | SharePoint type | Settings | Why |
|---|---|---|---|
| `Title` | Single line of text | **required** · **indexed** |  |
| `ConfigValue` | Single line of text | **required** |  |
| `Description` | Multiple lines of text (plain text, appending OFF) |  |  |
| `LastChangedBy` | Person or Group (people only) | _delete imported column and recreate_ |  |
| `AuthorityRef` | Single line of text |  | Credentials committee minute or instruction that authorised the current value |

## Document library — eCAF_Documents

1. Create a document library named `eCAF_Documents`, display name **CAF Documents**.
2. Site settings -> Site content types -> create each content type below, deriving
   from **Document**. Add the listed site columns, then add the content type to the
   library and enable "Allow management of content types" in library settings.
3. Structure is folder-per-provider. Create folders as providers are onboarded; the
   app's document staging screen writes into the folder matching the provider name.

| Content type | Columns |
|---|---|
| FPPE Product | Provider, Period, Preceptor, DocStatus |
| OPPE Product | Provider, Period, EndorsementComplete, UploadedToCCQAS, CCQASUploadDate |
| DHA455 PAR | Provider, TriggerEvent, Supervisor |
| DHA456 Signed | Provider, DateProviderSigned |
| Licensure-Cert | Provider, CertType, Expiration |
| EWP Checklist Artifact | Provider, ChecklistType, TransferEvent |
| Adverse-ForCause | Provider — **restricted: eCAF-MSP + PA only** |

`Adverse-ForCause` is **restricted to eCAF-MSP + PA only**. Break inheritance on
that content type's folder, or hold those documents in a separate library with its
own permissions. Library-level content-type permissions do not exist in SharePoint —
the boundary has to be a folder or a library, so decide which and record it.

## Permission groups

Create these five SharePoint groups before provisioning any list:

- `eCAF-MSP`
- `eCAF-ClinicalLeaders`
- `eCAF-Reviewers`
- `eCAF-Preceptors`
- `eCAF-Providers`

Baseline assignment (tighten per tenant SOP; this is the least-privilege starting
point, not a finished authorization):

| Group | Site | Providers | ReviewCases | Responses/Summaries | Standards | Config | RoleMap | Documents |
|---|---|---|---|---|---|---|---|---|
| eCAF-MSP | Read | Contribute | Contribute | Contribute | Contribute | Contribute | Contribute | Contribute |
| eCAF-ClinicalLeaders | Read | Read | Contribute | Read | Contribute | Read | Read | Contribute |
| eCAF-Reviewers | Read | Read | *item-level only* | Contribute | Read | Read | Read | None |
| eCAF-Preceptors | Read | Read | *item-level only* | Read | Read | Read | Read | Contribute |
| eCAF-Providers | Read | None | None | None | Read | Read | Read | *own folder only* |

**`eCAF_RoleMap` and `eCAF_Config` are MSP-write / everyone-read, and that is not
optional.** Both are read at app start and both change behaviour for every user:

- Write access to `eCAF_RoleMap` lets a user set their own `RoleName` to `MSP`.
  That alone only buys them a different menu — SharePoint still denies every read
  they are not entitled to, which is why the boundary is the group permissions and
  not the app. But it also removes the one honest record of who was granted what,
  so the quarterly F8 access review stops meaning anything.
- Write access to `eCAF_Config` lets a user set `OPPEReviewFloor` to `1`, at which
  point every provider in the MEF reads as having met the review floor, on screen
  and on generated documents. That is a credentialing-integrity control, not a
  preference. Leave versioning on so a change is attributable, and require the
  `AuthorityRef` column to name the committee minute that authorised it.

## Broken inheritance

Break inheritance on these lists **before importing any data**, so no item is ever
briefly visible site-wide:

- `eCAF_ReviewCases`
- `eCAF_ReviewResponses`
- `eCAF_ReviewSummaries`
- `eCAF_Routing`

On `eCAF_ReviewCases`, `eCAF_ReviewResponses`, `eCAF_ReviewSummaries` and
`eCAF_Routing`: remove all groups except **eCAF-MSP** at the list level. Reviewer
access is granted per item by flow F1 ("Stop sharing an item or file" then "Grant
access to an item or file"). A reviewer must not be able to enumerate cases that
are not theirs — that is the § 1102 boundary, and the app's own filtering is UX
only, not a control.

**Test this with two accounts before any real data is entered.** Sign in as test
reviewer A and confirm you cannot see test reviewer B's case, by direct item URL,
not just in the app.

## Indexes

Create these before the lists exceed 5,000 items — after the threshold, index
creation itself can fail and the fix gets ugly.

- `eCAF_Providers`: `Specialty`, `Department`, `PrivilegeExpiration`
- `eCAF_ReviewCases`: `Provider`, `ReviewPurpose`, `AssignedReviewer`, `Suspense`, `CaseStatus`
- `eCAF_ReviewResponses`: `ReviewCase`, `Standard`
- `eCAF_ReviewSummaries`: `ReviewCase`
- `eCAF_Standards`: `Specialty`, `Status`
- `eCAF_ActivityData`: `Provider`
- `eCAF_Routing`: `Document`, `CurrentHolder`
- `eCAF_ReviewerPool`: `Reviewer`
- `eCAF_Holidays`: `HolidayDate`
- `eCAF_RoleMap`: `Title`
- `eCAF_Config`: `Title`

## Versioning

Enable versioning on every list (major versions, keep 50). CLAUDE.md relies on
SharePoint item versioning for concurrency — there is no custom merge logic, so
version history is the only record of a lost update.

## Verification before you load real data

- [ ] `eCAF_Config` has all 7 keys and `DenominatorReviewTypes` = `Peer`.
- [ ] `eCAF_Holidays` has 66 rows and `HolidayDate` is Date Only.
- [ ] Every `note` column is Multiple lines of text with appending OFF.
- [ ] Every lookup resolves (open a `eCAF_ReviewResponses` item, confirm the
      Standard picker lists 26 standards).
- [ ] Two-account item-level permission test passed on `eCAF_ReviewCases`.
- [ ] No column named MRN, SSN, DOB, PatientName, or DateOfBirth exists on any
      list. `FIN` is the only encounter identifier. Run
      `python3 tools/lint_hard_rules.py` to check the artifacts; check the live
      site by eye.
- [ ] Sample rows deleted before go-live (they are fake, but they are also noise).

